<div class="loading">
  <div id="log"></div>
  <div id="progress"></div>
  <div class="cube">
    <div><img id="loading" data-src="/sky/sky-01.jpg" /></div>
    <div><img id="loading" data-src="/sky/sky-01.jpg" /></div>
    <div><img id="loading" data-src="/sky/sky-02.jpg" /></div>
    <div><img id="loading" data-src="/sky/sky-03.jpg" /></div>
    <div><img id="loading" data-src="/sky/sky-04.jpg" /></div>
    <div><img id="loading" data-src="/sky/sky-04.jpg" /></div>
  </div>
</div>

<style lang="scss">
  body.loaded {
    .loading {
      opacity: 0;
    }
  }
  .loading {
    pointer-events: none;
    transition-duration:0.4s;
    transition-timing-function: ease-in;
    transition-delay: 0.2s;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    z-index: 9999;
    background-color: var(--gray);
    display: grid;
    place-items: center;
  }
  #log {
    position: absolute;
    opacity: 0;
  }
  #progress {
    position: absolute;
    width: fit-content;
    bottom: 10rem;
    right: 10rem;
    color: var(--white);
    font-size: 2.4rem;
  }
  .cube {
    position: relative;
    width: 20rem;
    aspect-ratio: 1/1;
    transform-style: preserve-3d;
    animation: rotateCube 10s infinite linear;
  }
  .cube div {
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid var(--black);
    backface-visibility: visible;
  }
  .cube > div > img {
    width: 100%;
    height: 100%;
    display: block;
  }

  .cube > div:nth-child(1) {
    transform: rotateY(0deg) translateZ(10rem);
  }
  .cube > div:nth-child(2) {
    transform: rotateY(180deg) translateZ(10rem);
  }
  .cube > div:nth-child(3) {
    transform: rotateY(90deg) translateZ(10rem);
  }
  .cube > div:nth-child(4) {
    transform: rotateY(-90deg) translateZ(10rem);
  }
  .cube > div:nth-child(5) {
    transform: rotateX(90deg) translateZ(10rem);
  }
  .cube > div:nth-child(6) {
    transform: rotateX(-90deg) translateZ(10rem);
  }
  @keyframes rotateCube {
    0% {
      transform: rotateX(0deg) rotateY(0deg);
    }
    25% {
      transform: rotateX(90deg) rotateY(90deg);
    }
    50% {
      transform: rotateX(180deg) rotateY(180deg);
    }
    75% {
      transform: rotateX(270deg) rotateY(270deg);
    }
    100% {
      transform: rotateX(360deg) rotateY(360deg);
    }
  }
</style>
