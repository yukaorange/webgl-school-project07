---
---

<div class="container">
  <slot />
</div>

<style lang="scss">
  .container {
    margin-inline: auto;
    max-width: var(--max-width);
  }
  .container.dark {
    background-color: var(--dark);
  }
</style>
